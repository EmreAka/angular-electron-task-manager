import { SecondsToTimePipePipe } from './seconds-to-time-pipe.pipe';

describe('SecondsToTimePipePipe', () => {
  it('create an instance', () => {
    const pipe = new SecondsToTimePipePipe();
    expect(pipe).toBeTruthy();
  });
});
